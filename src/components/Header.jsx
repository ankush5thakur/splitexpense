import React from "react";
import "./Header.css"; // Create a corresponding CSS file for styling

const Header = () => {
  return (
    <header className="header">
      <h1 className="header-title">SplitPocket</h1>
    </header>
  );
};

export default Header;
